# Pinned so a training run is reproducible. Bump deliberately, and re-run
# test_parity.py afterwards.
#
# numpy is held below 2 by mediapipe, which is the only thing here that cares.
# This file pinned numpy==2.2.1 against mediapipe 0.10.21, which requires
# numpy<2 — an unsatisfiable set, so `pip install -r requirements.txt` had never
# once succeeded and nobody following the README could have set the pipeline up
# at all. Found by the first CI run this repo has ever done.
numpy==1.26.4
torch==2.6.0
onnx==1.17.0
onnxruntime==1.20.1
mediapipe==0.10.21
opencv-python-headless==4.11.0.86
scikit-learn==1.6.1
tqdm==4.67.1
pandas==2.2.3
